# **Localhost Div Scanner Extension**

A Chrome extension that scans all localhost webpages for a specific `div` with `id="my_text"`, sends its content to a Node.js server, and logs the response in the console.

---
